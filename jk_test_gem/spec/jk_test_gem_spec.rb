require "spec_helper"

describe JkTestGem do
  it "has a version number" do
    expect(JkTestGem::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
